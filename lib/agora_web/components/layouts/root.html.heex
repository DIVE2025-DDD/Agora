<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- 기본 메타 태그 -->
    <title>{assigns[:meta_attrs][:title] || "Agora - 시민 토론 플랫폼"}</title>
    <meta
      name="description"
      content={assigns[:meta_attrs][:description] || "모든 시민이 함께 모여 소통하고 토론하는 공간"}
    />
    
<!-- Open Graph 메타 태그 (카카오톡용) -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={assigns[:meta_attrs][:title] || "Agora - 시민 토론 플랫폼"} />
    <meta
      property="og:description"
      content={
        assigns[:meta_attrs][:description] || "모든 시민이 함께 모여 소통하고 토론하는 공간, 아고라에서 당신의 목소리를 들려주세요."
      }
    />
    <meta property="og:image" content={assigns[:meta_attrs][:image] || url(~p"/images/logo.png")} />
    <meta property="og:url" content={assigns[:meta_attrs][:url] || url(~p"/")} />
    <meta property="og:site_name" content={assigns[:meta_attrs][:site_name] || "Agora"} />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.inertia_title>{assigns[:page_title]}</.inertia_title>
    <.inertia_head content={@inertia_head} />
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script type="module" defer phx-track-static src={~p"/assets/app.js"} />
  </head>
  <body>
    {@inner_content}
  </body>
</html>
